# ECDSA-Signature-Verifier-Compressed-Public-Key-
> ⚙️ Verifies an ECDSA `(r, s)` signature for a given message hash (`z`)   > using a **compressed public key (02/03 format)** on the SECP256k1 curve.
